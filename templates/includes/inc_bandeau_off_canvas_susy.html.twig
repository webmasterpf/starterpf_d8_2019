        {% if bandeau1 is not empty %} 
    <div class="transformer">
        <div class="off-canvas">
        <div class="close">
            
            <img class="close-btn" 
                 src="../ {% set theme_path = 'undefined' %} {{ theme_path~'/images/close.svg' }}" alt="Fermer" title="Fermer">
            
        </div>
        <div class="bandeau-pagetop">
            {{ bandeau1 }} 
        </div>
            </div>
        <a href="#" class="menu-toggle">Les lycées de Provence Formation</a>

    {% endif %}